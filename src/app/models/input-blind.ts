export class InputBlind {

  blindId: number;
  blindPosition: number;

  constructor(blindId: number, blindPosition: number) {
    this.blindId = blindId;
    this.blindPosition = blindPosition;
  }

}
